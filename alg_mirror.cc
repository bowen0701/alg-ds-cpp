#include <iostream>
#include <queue>
#include <stack>

// Mirror a queue and append to it. 
// Example: ['a', 'b', 'c'] => ['a', 'b', 'c', 'c', 'b', 'a']
std::queue Mirror(std::queue) {
	// TODO: implement Mirror().
}


int main() {
	return 0;
}
